---
id: run-prover
title: "Running a Prover"
description: The step to run a prover
---

## Using docker

To run a prover on a machine use below docker command. This command will start the Lagrange Network prover container in detached mode, allowing it to run in the background while generating zero-knowledge proofs.

```bash
docker compose up -d
```
